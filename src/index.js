import React from "react";
import ReactDOM from "react-dom/client";
import BusinessCard from "./BusinessCard";

const root = document.getElementById("root");

ReactDOM.createRoot(root).render(<BusinessCard />);
